## 0. 核心原力：物理边界守卫 (Physical Guard)
- **Framework Authority**: 绝对遵守 `AOS_Framework_Core.md` 与 `AOS_V4_Recursive_Alignment_Manual.md`。
- **Automated Enforcement**: 任何 `write_to_file` 操作后，AI **必须自动运行** `python scripts/aos_boundary_guard.py`。
- **Hard Break**: 若脚本返回非零状态（FAIL），AI 必须立即停止任务并尝试自修复或报告越界详情。

## 1. 顶层指令与递归树 (The Execution Tree)
- **`[GO] <Intent>`** -> **全自动执行与物理审计链**：
  1. 意图蒸馏 -> 2. 执行实现 -> 3. **`scripts/aos_boundary_guard.py` (后置审计)** -> 4. `[CHECK]` (业务验证)。
- **`[PLAN]`** -> 输出符合 AAA 标准的任务树。

- **`[SYNC]`** -> 对齐 DNA。

## 2. 行为准则 (The Leaf SOPs)
- **AAA 强制化**: 单点验证。
- **预期对齐 (Pre-Exec)**: 输出预期行为矩阵。
- **证据闭环 (Post-Exec)**: 物理日志对齐。
- **[SELF_AUDIT] 输出**: 每次文件变动后，必须在回复中包含《AOS 边界审计报告》。

## 3. 标准钩子定义 (Module Reference)
- **`[EXEC]`** / **`[CHECK]`** / **`[STATUS]`** / **`[DISTILL]`** 等钩子保持 V3.5 定义。
- **新增 `[SELF_AUDIT]`** -> 变更完整性与边界校验。

## 4. 工程红线 (Engineering Red Lines)
- **禁止臆想验证**: 无物理日志不成功。
- **禁止破坏性覆盖**: 严禁删除有意义的历史代码/注释。
- **三失败熔断**: 强制人类接入。




