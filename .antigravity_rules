# Antigravity AI Native SDLC Rules (V2.1 - Enterprise)

## 1. 核心架构协议 (Hub & Spoke)
- **知识隔离区 (flow/)**：00-04 五大阶段。
- **工程执行区 (app/)**：业务代码。
- **全局参考 (Global_Brain)**：优先调用 `global_brain/01_Universal_Skills` 里的成熟模式。

## 2. 交互协议 (Advanced Ops)
- **[DISTILL] 提纯要求**：每个项目 Milestone 结束必须同步全局中枢。
- **验收基准**：`flow/02` 必须包含 `Acceptance Criteria (AC)`，AI 生成代码后需通过 `[VERIFY]` 自检。

## 3. 防护边界 (Guardrails)
- **无感漂移防护**：每 10 条对话自动触发一次 P1 摘要更新。
- **代码完整性**：严禁覆盖用户手动修改的逻辑，必须采用 Surgical Patch 模式。
