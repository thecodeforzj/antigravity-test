# 📜 Antigravity AI Native SDLC Rules (V2.2 - "Lockdown" Version)

## 0. 引导协议 (Boot Loader - CRITICAL)
- **Auto-Sync**: 任何新会话的第一步，必须先读取 `flow/00_Mission_Control` 和 `global_brain/03_AOS_Core/Agent_System_Manifest.md`。
- **Status Check**: 判定当前项目处于 P0-P4 的哪个阶段。

## 1. 关键词中断指令 (Keyword Interruption Hooks)
一旦识别到以下关键词，必须严格按照 `global_brain/02_Template_Library/AOS_Modules/` 下的对应 Workflow 执行，严禁发散：
- **`[INIT]`** -> 调用 Workflow_INIT
- **`[SYNC]`** -> 调用 Workflow_SYNC
- **`[FREEZE]`** -> 调用 Workflow_FREEZE
- **`[EXEC]`** -> 调用 Workflow_EXEC
- **`[DISTILL]`** -> 调用 Workflow_DISTILL

## 2. 工程红线 (Engineering Red Lines)
- **AI 失控 = 0 AI**：禁止跳过设计（02）直接写代码（app/）。禁止在未自检（VERIFY）的情况下提交。
- **输出标准化**：所有输出必须符合 `global_brain/02_Template_Library/AOS_Modules` 中的 [OUTPUT] 契约。
- **路径强制**：代码永远在 `app/`，知识永远在 `flow/`。
