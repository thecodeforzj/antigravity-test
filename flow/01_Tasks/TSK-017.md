# Task TSK-017: Regression Suite CLI Enhancement

## Objective
Enhancing the `aos_full_regression.py` script to support targeted testing. This allows developers to run specific test cases (e.g., `FMA_BASIC`) without executing the entire suite, improving turnaround time during iterative development.

## Key Requirements
- [ ] **T1: Selective Testing CLI**: Implement command-line argument parsing in the regression engine.
- [ ] **T2: Targeting Logic**: Update the execution loop to filter the `TEST_CASES` list based on user input.

## Success Criteria
- [ ] `python3 scripts/aos_full_regression.py FMA_BASIC` only executes the FMA case.
- [ ] Running with no arguments still triggers full-coverage regression.
- [ ] Sync protocol PASS.

## 📦 [ARTIFACT_INDEX]
- `scripts/aos_full_regression.py`
- `flow/01_Tasks/TSK-017.md`

---
**Status: ACTIVE** | **AOS V3.5-RIGOR**
