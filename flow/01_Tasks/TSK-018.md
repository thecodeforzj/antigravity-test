# Task TSK-018: Regression Artifact Discretization

## Objective
Fixing the artifact collision in the regression suite where different test cases overwrite the same result and report files, leading to incorrect validation and confusion.

## Key Requirements
- [ ] **T1: Dynamic Result Output**: Update `solve_macro_vma.py` to accept an optional `--output` path.
- [ ] **T2: Regression Engine Mapping**: Update `aos_full_regression.py` to use unique file paths for each test case (e.g., `flow/03_Output/{CASE_ID}_Result.json`).
- [ ] **T3: Report Customization**: Update `pseudo_gen.py` to accept an optional `--report` path.

## Success Criteria
- [ ] Running `FMA_BASIC` produces `flow/03_Output/FMA_BASIC_PseudoCode.txt`.
- [ ] Running `HORNER_6TH` produces `flow/03_Output/HORNER_6TH_PseudoCode.txt`.
- [ ] `git status` shows all these new reports as officially tracked in TSK-018.

---
**Status: ACTIVE** | **AOS V3.5-RIGOR**
